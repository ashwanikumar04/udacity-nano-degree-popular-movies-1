<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:angle="90"
        android:centerColor="@color/white"
        android:centerX="0.75"
        android:endColor="@color/white"
        android:startColor="@color/white" />

    <!-- Specify a semi-transparent solid green background color -->


    <!-- Specify a dark green border -->
    <stroke
        android:width="@dimen/unit_5"
        android:color="@color/white" />

    <!-- Specify the margins that all content inside the drawable must adhere to -->
    <padding
        android:bottom="@dimen/unit_30"
        android:left="@dimen/unit_30"
        android:right="@dimen/unit_30"
        android:top="@dimen/unit_30" />

</shape>